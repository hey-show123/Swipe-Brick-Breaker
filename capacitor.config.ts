import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.swipebrickbreaker.app',
  appName: 'Swipe Brick Breaker',
  webDir: 'dist'
};

export default config;
